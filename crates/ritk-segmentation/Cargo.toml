[package]
name = "ritk-segmentation"
version = "0.3.0"
description = "Medical image segmentation algorithms for RITK"
keywords.workspace = true
categories.workspace = true
homepage.workspace = true
readme.workspace = true
authors.workspace = true
repository.workspace = true
publish = true
edition = "2021"
license.workspace = true

[dependencies]
ritk-core = { path = "../ritk-core", version = "0.10.0" }
ritk-image = { path = "../ritk-image", features = ["test-helpers"], version = "0.4.0" }
ritk-filter = { path = "../ritk-filter", version = "0.4.0" }
ritk-tensor-ops = { path = "../ritk-tensor-ops", version = "0.2.0" }
anyhow = { workspace = true }
tracing = { workspace = true }
coeus-core = { workspace = true }
coeus-tensor = { workspace = true }
moirai = { workspace = true }
leto = { workspace = true }
mnemosyne = { workspace = true }
leto-ops = { workspace = true }

[dev-dependencies]
base64 = { workspace = true }
image = { workspace = true }
# The level-set rejection tests import `ritk_core::rejection`, which the
# feature gates; without naming it here a `-p ritk-segmentation` test build
# fails while workspace-wide feature unification (ritk-filter,
# ritk-registration) hides the gap. Same pattern as those members.
ritk-core = { path = "../ritk-core", features = ["test-helpers"], version = "0.10.0" }
ritk-spatial = { path = "../ritk-spatial" }  # path-only: dev-deps form a publish cycle
[lints]
workspace = true

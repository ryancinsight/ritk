[package]
name = "ritk-model"
version = "0.1.0"
edition = "2021"

[dependencies]
ritk-core = { path = "../ritk-core" }
burn = { version = "0.19", features = ["wgpu", "train", "autodiff"] }
anyhow = "1.0"
tracing = "0.1"

[dev-dependencies]
burn-ndarray = "0.19"

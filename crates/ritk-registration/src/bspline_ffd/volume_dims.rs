//! Volume dimensions — re-exported from `ritk_core::spatial`.
//!
//! `VolumeDims` is defined in `ritk-core` and re-exported here so that
//! `ritk-registration` consumers can continue to use the `bspline_ffd`-relative
//! path without a breaking change.
pub use ritk_core::spatial::VolumeDims;

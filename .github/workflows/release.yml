# Multi-platform wheel build and PyPI publish.
#
# Triggered on any v*.*.* tag push.
# Builds wheels for Linux (manylinux x86_64), Windows (x86_64-msvc), and macOS (universal2).
# Publishes to PyPI via OIDC trusted publishing after all wheels are built.
#
# Requirements:
#   - A PyPI "pypi" environment configured in GitHub repository settings with
#     trusted publishing for this workflow (OIDC; no API token required).
#   - MSYS2 at C:/msys64/ucrt64/bin must be present on windows-latest runners
#     (GitHub-hosted runners include it by default).

name: Release Wheel

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  build-wheel:
    name: Build wheel (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64
            manylinux: auto
          - os: windows-latest
            target: x86_64
            manylinux: "off"
          - os: macos-latest
            target: universal2-apple-darwin
            manylinux: "off"

    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          path: ritk
          persist-credentials: false

      - name: Checkout provider dependencies
        uses: ./ritk/.github/actions/checkout-atlas-path-dependencies
        with:
          manifest: ritk/Cargo.toml
          destination: .

      - name: Add MSYS2 ucrt64 to PATH (Windows — clang-cl C++ runtime DLLs)
        if: runner.os == 'Windows'
        shell: bash
        run: echo 'C:/msys64/ucrt64/bin' >> $GITHUB_PATH

      - name: Build wheels
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b
        with:
          target: ${{ matrix.target }}
          args: >-
            --release
            --manifest-path ritk/crates/ritk-python/Cargo.toml
            --out ritk/dist
          manylinux: ${{ matrix.manylinux }}
          sccache: true

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.target }}
          path: ritk/dist/

  publish:
    name: Publish to PyPI
    needs: build-wheel
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # Required for OIDC trusted publishing
    steps:
      - name: Download all wheel artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
        with:
          packages-dir: dist/
          skip-existing: true
